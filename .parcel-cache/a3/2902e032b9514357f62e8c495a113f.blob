"use strict";

function sing(song, song2) {
    console.log(song);
    console.log(song2);
}

sing(`llalalala`, `dfjdfjdjfjd`);

function times(a, b) {
    const sum = a * b;
    console.log(sum);
    return sum;
}

const news = times(11, 2);

function total() {
    console.log(news + 100);
    return news + 100;
}

// alert(total());

const game = function (a, b) {
    console.log(a, b);
};

game(`ayo`, `olanide`);

const sum2 = (a, b) => {
    console.log(a + b);
};

sum2(1, 1);

const name = (a, b) => {
    console.log(`ayo`);
};

name();

const name2 = function (a, b) {
    console.log(`fumi`);
};

name2();

let list = [`ayo`, `name`, `fumi`, `game`];

console.log(list[3]);

console.log(list);

list.shift();
console.log(list);

list.pop();
console.log(list);
list.push(`drake`);
console.log(list);

const map = list.concat([`dam`, `dear`, `miz`, `got`]);

console.log(list);

console.log(map);

const app = map.sort();

console.log(app);

let user = {
    name: `ayo`,
    dob: `september`,
    car: `tesla`,
    age: 30,
    spells: [`ice`, `fire`, `wind`, `air`],
};

console.log(user);

console.log(user.age);

user.age = 21;

console.log(user.age);

console.log(user.spells[1]);

let users = [
    {
        username: `amy`,
        password: `map`,
    },

    {
        username: `mac`,
        password: `deep`,
    },
];

let newsFeed = [
    {
        username: `amy`,
        timeline: `hey man im tired`,
    },

    {
        username: `pat`,
        timeline: `i love bitcoin!`,
    },
];

let userNamePromt = prompt(`what is your user name`);
let password = prompt(`what is your password`);

if ((userNamePromt = `amy` && users[1].password === `map`)) {
    console.log(newsFeed[0].timeline);
} else {
    console.log(`wrong credentials`);
}
